<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <title>Document</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
      <div class="flex w-full justify-end">
        <a
          href="/logout"
          class="bg-red-500 rounded-md px-3 py-2 txt-sm mb-5 inline-block"
          >Logout</a
        >
      </div>
      <h3 class="text-3xl mb-5">
        <span class="font-light">Hello</span> , <%= user.name %> 👋🏻
      </h3>
      <h5 class="mb-5">You can create a new post</h5>
      <form action="/post" method="post">
        <textarea
          class="resize-none bg-tranparent border-2 border-zinc-800 rounded-md w-1/3 p-3 outline-none"
          placeholder="Whats's on your mind ?"
          name="content"
          id=""
        ></textarea>
        <input
          type="submit"
          value="Create new post"
          class="px-3 py-2 bg-blue-500 block rounded-md m-2 w-40"
        />
      </form>
      <div class="poast mt-20">
        <h3 class="text-zinc-400">Your Poast</h3>
        <div class="post-container">
          <% user.posts.reverse().forEach((post)=>{ %>

          <div
            class="poast w-1/3 p-5 border-[1px] rounded-md mt-4 border-zinc-700 bg-zinc-800"
          >
            <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
            <p class="text-sm tracking-tight">
                <%= post.content %>
            </p>
            <small class="inline-block mt-4"><%= post.likes.length %> likes</small>
            <div class="flex gap-4 btns mt-5">
              <a class="text-blue-500" href="/like/<%= post._id %>"><%= post.likes.indexOf(user._id) === -1 ? "Like" : "unlike" %></a>
              <a class="text-zinc-600" href="/edit/<%= post._id %>">Edit</a>
              <a class="text-red-600" href="/delete/<%= post._id %>">delete</a>
            </div>
          </div>

          <% }) %>
        </div>
      </div>
    </div>
  </body>
</html>
